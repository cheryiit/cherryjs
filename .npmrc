# CherryJS npm config
#
# legacy-peer-deps: some @convex-dev/* packages have peer dep conflicts
# between their internal workpool/rate-limiter versions. This flag
# prevents install from failing on those mismatches. Safe because
# Convex components run in isolated namespace — they don't share code
# at runtime.
legacy-peer-deps=true
